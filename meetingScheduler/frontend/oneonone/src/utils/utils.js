// This file is for adding any util functions (e.g. string separation functions; date conversion, etc)

// import {fetchContacts} from "../scenes/createSchedules/queries";
//
// export const fetchContactName = async (contactId) => {
//     try {
//         const contacts = await fetchContacts();
//         const contact = contacts.find(contact => contact.id === contactId);
//         console.log("contact", contact)
//         return contact ? contact.name : "Unknown";
//     } catch (error) {
//         console.error("Error fetching contact name:", error);
//         return "Unknown";
//     }
// };